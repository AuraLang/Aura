import interpreter.tokens

checkExists: bool = False

def check(tokens: interpreter.tokens.TokenList, index: int, pattern: list):
    # TO DO
    return True

def isValid(tokens: interpreter.tokens.TokenList) -> bool:
    # TO DO
    return True